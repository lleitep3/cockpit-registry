#!/usr/bin/env bash
set -euo pipefail

command -v bash >/dev/null || { echo "bash is required" >&2; exit 1; }
command -v terraform >/dev/null || echo "Warning: terraform is not installed; package CLI remains available for scaffolding and static checks."
echo "cloud-infrastructure configuration is valid."
